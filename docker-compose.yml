name: medatlas

services:
  redis:
    image: redis:8-alpine

    ports:
      - "127.0.0.1:6380:6379"

    volumes:
      - redis_data:/data

    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M

    restart: unless-stopped

volumes:
  redis_data:
